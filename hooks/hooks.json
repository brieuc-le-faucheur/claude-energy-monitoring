{
  "description": "Energy Monitor — met à jour le cache de stats après chaque réponse Claude",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/update-stats.sh",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
